import { PlaceholderPanel } from "../_shell/PlaceholderPanel";

/** `/refunds` — 사이드바 자리표. 담당 task 가 실제 화면으로 교체한다(경로는 T0.5에서 확정). */
export default function Page() {
  return <PlaceholderPanel href="/refunds" />;
}
