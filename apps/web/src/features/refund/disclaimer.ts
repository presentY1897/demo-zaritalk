/**
 * "실제 세법 자문이 아님" 고지 (T2.3) — 계산기·신청서가 **같은 문구**를 쓴다.
 *
 * 원래 `RefundCalculatorView.tsx` 안에 있었는데, T2.4 신청 화면도 같은 고지를 띄워야 해서
 * 순수 모듈로 뺐다(화면 컴포넌트를 import 해서 상수만 꺼내 쓰면 번들이 딸려 온다).
 */
export const REFUND_DISCLAIMER =
  "이 계산기는 데모용 추정치입니다. 실제 세법 자문이 아니며, 실제 공제 가능 금액은 소득·부양가족·주택 요건과 연도별 세법 개정에 따라 달라집니다.";
