# Phase 6 — 그로스 마무리·대시보드

> 선행: D2(A/B 소재) 결정
> 모델: TrackingEvent · AbAssignment

## ⬜ T6.1 A/B 실험 실운영

- **내용**: `notice_cta`(D2 제안) — 공개 고지서(T1.8) 가입 CTA 문구·배치 2안. anonId 해시 고정 배정. 퍼널: `notice_view → notice_cta_click → signup_start → signup_complete`

| 엔드포인트 | 동작 |
|---|---|
| `GET /api/ab/[experimentKey]` | 변형 배정 조회/생성(anonId 고정, 로그인 시 userId 연결) |

- **완료 기준**
  - [ ] 같은 브라우저 항상 같은 변형, 변형별 노출·클릭 이벤트 구분 수집
- **테스트** — 최소(**핵심**): ①해시 배정 결정성 ②대량 샘플 분포 약 50:50 ③userId 연결 유지 / 통합: E2E 신규 브라우저 컨텍스트 2개에서 각자 변형 고정

## ⬜ T6.2 어드민 지표 대시보드·퍼널

| 라우트/API | 동작 |
|---|---|
| `/` (admin) | 가입자·DAU 추이(TrackingEvent), 수납률(청구 대비 납부, 월별), 발송·열람률, 결제액 추이, 환급 파이프라인, A/B 퍼널 차트(변형별 전환율) |
| `GET /api/admin/metrics/overview` `/funnel` | 대시보드 집계(각 1개 엔드포인트로 묶음) |

- **완료 기준**
  - [ ] 시드+E2E 실데이터가 차트에 표시, 퍼널이 변형별 분리
- **테스트** — 최소: 수납률 계산 · 퍼널 단계 카운트(anonId 중복 제거)

## ⬜ T6.3 어드민 조회 화면 일괄

| 라우트(admin) | 화면·기능 |
|---|---|
| `/users` | 검색(이름·전화), 상세: 프로필·계약·신청 이력 타임라인 |
| `/leases` `/charges` | 상태 필터, 연체 계약 드릴다운 |
| `/messages` | 발송 이력 전체 — 종류·수신자 필터, 열람 여부, 본문 미리보기(알림톡 스타일) |
| `/events` | 이벤트 로그 — 이름·기간 필터, 시간대별 카운트 차트 |

- 어드민 API는 `/api/admin/*` 네임스페이스, isAdmin 미들웨어 가드
- **완료 기준**
  - [ ] 전 화면 서버 페이지네이션·필터 동작, 비어드민 차단
- **테스트** — 최소: admin 가드 — isAdmin 아닌 세션 403

## ⬜ T6.4 SEO 마무리

- **내용**: 공개 페이지 3종(고지서·매물 상세·환급 계산기) 메타·OG·JSON-LD 점검, sitemap.xml·robots.txt
- **완료 기준**
  - [ ] 공개 페이지 3종 Lighthouse SEO 90+
- **테스트** — 최소: sitemap에 공개 라우트 포함

## ⬜ T6.5 문서·마무리

- **내용**: README(아키텍처 다이어그램·데모 계정·로컬 실행·배포 가이드), 데모 시나리오 대본(시연 순서), task 상태 최종 갱신
- **완료 기준**
  - [ ] README만 보고 클론→실행→데모 로그인 가능
- **테스트**: 전체 E2E 스위트 최종 green(회귀 확인)

## Phase 완료 조건

- [ ] 전 task ✅
- [ ] 전체 E2E green + 라이브 URL 최종 점검
- [ ] 데모 시나리오 대본대로 전 역할 시연 가능
