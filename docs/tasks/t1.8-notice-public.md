# T1.8 공개 고지서 페이지 (그로스 핵심)

> Phase 1 · 상태: ⬜ · [목차](./README.md)

| 라우트/API | 동작 |
|---|---|
| `/notice/[token]` | **비로그인 공개** — 청구 내역 상세, 납부 계좌 안내(더미), 임대인 메시지, 하단 「자리톡으로 월세 관리하기」 가입 CTA(D2 A/B 대상), OG·SEO 메타 |
| `GET /api/notices/[token]` | 공개 데이터, 첫 조회 openedAt 기록 + `notice_view` 트래킹 |

- **완료 기준**
  - [ ] 시드 토큰 `demo-notice-hong` 비로그인 열람 → 임대인 이력에 "열람" 반영
- **테스트** — 최소: 잘못된 토큰 404 · openedAt 최초 1회만 / 통합: **E2E(그로스 여정)** 발송 → 비로그인 열람 → CTA → 가입 → 수락, `notice_view→notice_cta_click→signup_*` 이벤트 순서 확인
